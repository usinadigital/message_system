--- menu button: update categories (cosi non devo fare l´update automatico)
--- quando invio un msg das WS lo invio a tutti, é android che vede se le cat sono accettate
	altrimenti devo anche fare anche il metodo che dice al WS le nuove cat dell´utente.
